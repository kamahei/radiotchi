Use case: stylized-concept
Asset type: Radiotchi 1-bit virtual pet idle animation sprite sheet
Input image: the visible reference image is the exact neutral static design reference, left to right: egg, baby turtle, baby rabbit, baby cat, baby bird, baby owl.
Primary request: Create a clean animation source sheet with exactly 6 rows and 4 columns. Each row is one character from the reference image in the same order; each column is one frame of a 4-frame idle loop.
Style/medium: cute Tamagotchi-like 1-bit pixel sprite line art, pure black lines on pure white cells, thin crisp 1-pixel-like outlines, open white interiors.
Composition/framing: each cell contains one centered 64x64-style sprite with generous white margin. Use a thin light-gray grid to separate cells. No text labels.
Animation directions: column 1 neutral pose matching the reference; column 2 small pose change; column 3 main idle pose; column 4 return pose. Egg changes only its tilt/shape slightly, with intact shell lines and no extra marks. Baby turtle moves only head/legs slightly. Baby rabbit changes only ear pose. Baby cat blinks and changes only tail pose. Baby bird changes only wing pose. Baby owl blinks and changes only wings.
Constraints: preserve each character identity and silhouette from the reference. Do not crop. Do not distort bodies. Do not use disconnected line fragments. ABSOLUTELY NO motion lines, no speed marks, no vibration marks, no sparkle marks, no emphasis marks, no extra marks outside the body. Do not add labels, numbers, UI, shadows, gray shading, color, or background art. Output should be a single contact sheet only.